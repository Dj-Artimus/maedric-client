/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://maedric.com',
    generateRobotsTxt: true, // Will generate robots.txt too (optional since you made your own)
};
